const initialState = {

}
const modalReducer = (state = initialState, action) => {
    switch (action.type) {
        case '':
            return
        default:
            return state
    }
}

export default modalReducer;